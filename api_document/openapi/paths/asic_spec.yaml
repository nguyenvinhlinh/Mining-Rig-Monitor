post:
  tags:
    - ASIC
  summary: Submit ASIC specification
  description: "This endpoint helps submitting an ASIC specification"
  operationId: post-asic-spec
  security:
    - mining_rig_code: []
  responses:
    '200':
      description: OK with no response body

    '401':
      description: Invalid mining rig code
      content:
        application/json:
          schema:
            $ref: ../components/schemas/401_Unauthorized.yaml
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/AsicSpec.yaml
    description: "Asic specification"
    required: true
